# =============================================================================
# DATABASE (Neon Postgres)
# =============================================================================
# Get this from Neon dashboard: https://console.neon.tech
# Use the "pooled" connection string for serverless
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/preu?sslmode=require

# =============================================================================
# ANALYTICS (PostHog)
# =============================================================================
# PostHog Analytics (optional - analytics disabled if not set)
# Get these from https://app.posthog.com/project/settings
NEXT_PUBLIC_POSTHOG_KEY=phc_your_project_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Build version for analytics (automatically set by Vercel)
NEXT_PUBLIC_BUILD_VERSION=local

# =============================================================================
# EMAIL (Resend)
# =============================================================================
# Resend API key for transactional emails (optional - emails disabled if not set)
# Get this from https://resend.com/api-keys
# IMPORTANT: You must verify your sending domain in Resend dashboard
RESEND_API_KEY=re_your_api_key

# =============================================================================
# APPLICATION
# =============================================================================
# Base URL for links in emails and redirects
# Vercel sets this automatically, but you can override for custom domains
NEXT_PUBLIC_BASE_URL=https://preu.arbor.school
