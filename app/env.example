# Local development database (Homebrew PostgreSQL)
# Setup: brew install postgresql@15 && brew services start postgresql@15
#        createdb preu && createuser preu_app
DATABASE_URL=postgresql://preu_app@localhost:5432/preu

# PostHog Analytics (optional - analytics disabled if not set)
# Get these from https://app.posthog.com/project/settings
NEXT_PUBLIC_POSTHOG_KEY=phc_your_project_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Build version for analytics (optional - set in CI/CD)
# Used to track which release events came from
NEXT_PUBLIC_BUILD_VERSION=local
